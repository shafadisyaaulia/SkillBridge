# Path ke folder model (relatif dari backend/, atau absolute path)
MODELS_DIR=models

# Batas ukuran upload CV dalam MB
MAX_UPLOAD_MB=10

# Port server (digunakan oleh uvicorn saat run manual)
PORT=8000
HOST=0.0.0.0
